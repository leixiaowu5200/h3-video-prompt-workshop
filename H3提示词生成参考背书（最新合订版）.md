# H3 提示词生成参考背书（最新合订版）

> 本文件是「H3 AI 视频提示词工坊」生成逻辑的**权威参考背书（single source of truth）**。
> 任何一次提示词生成都应可追溯到底层哪条规则 / 哪份资料。本文合订：
> **① MiniMax 官方三流程模板（5/10/15 秒）** ＋ **② MiniMax H3 英文输出规范（I2VA/T2VA 三字段）** ＋ **③ script-writing-studio 导演内部规范** ＋ **④ 巨晴水疗产品合规语境**。
> 最后更新：2026-08-11。

---

## 0. 资料清单（本次合订来源）

| 编号 | 资料 | 性质 | 在本背书中的角色 |
|---|---|---|---|
| S1 | `MiniMax H3 5秒视频提示词三流程模板.docx` | 官方模板 | 三流程方法论（5s 时长预算） |
| S2 | `MiniMax H3 10秒视频提示词三流程模板.docx` | 官方模板 | 三流程方法论（10s 时长预算） |
| S3 | `MiniMax H3 15秒视频提示词三流程总模板.docx` | 官方模板 | 三流程方法论（15s 时长预算） |
| S4 | 用户粘贴的官方 I2VA 示例（指令行＋三字段＋[Shot N]） | 官方示例 | 英文输出格式范本 |
| S5 | `h3-prompt-writing` skill（`base-en.txt` / `ref-en.txt`） | 官方 skill | T2VA/I2VA/FL2VA/L2VA/Ref2VA 字段与顺序 |
| S6 | `script-writing-studio` skill（导演内部专用 V2.0） | 导演规范 | 素材锚定 / 台词时长闸门 / 段末状态 / 防跑偏 |
| S7 | 巨晴水疗设备（hydro 行业，data.js 内置） | 产品语境 | 合规「调理不治疗」话术与养生知识 |
| S8 | 工坊 `data.js` / `app.js` 当前实现 | 落地代码 | 已落地的生成逻辑（见 §7 映射） |

---

## 1. 核心方法论：MiniMax H3 三流程（文戏 / 武戏 / 九宫格）

来源：S1/S2/S3。这是**上游叙事模式选择**——先定走哪条流程，再压缩成可直接进 H3 的成品提示词。

### 1.1 首次入口：必须先确定流程
- 用户未明确模式时，**第一条回复只问模式、不生成提示词**：
  ```
  请选择这次要走的流程：
  1. 文戏模式：人物关系、对白、微表情、情绪转折与剧情结果。
  2. 武戏模式：高密度打斗、连续攻防、快慢节奏、受力与反制。
  3. 九宫格模式：先生成 3×3 关键画面，再根据实际九宫格生成对应的 H3 视频提示词。
  ```
- 用户已说「文戏/武戏/九宫格」或对应数字 → 不再追问；已同时给模式＋故事/图片 → 立即生成。
- 先给了故事/图片但没选模式 → 保留素材、只追问模式，沿用已收内容。

### 1.2 输入方式（四类）
1. **只有粗略故事**：一句也行。提取主体/关系/目标/阻碍/关键动作/情绪转折/结局/风格线索；缺普通美术细节自行合理决定，不连环追问。
2. **只有一张图片**：观察人物数量、外貌、服装、道具、环境、时代、材质、光线、色彩、构图；图片锁定可见身份与视觉世界，模式推导动作与叙事。
   - 文戏：从姿态/关系距离/道具/环境推导克制可理解的关系转折。
   - 武戏：锁定主角与装备；无对手则设计轮廓明显不同的对手。
   - 九宫格：把图视为视觉母版/主体参考，派生九个连续但构图不同的关键状态。
3. **图片＋故事**：图片锁人物/物体/服装/材质/场景/色彩；故事锁事件顺序/关系/动作结果/结尾。冲突时优先保留图片可见身份特征，故事决定后续事件。
4. **只补问一个简短问题**的情形（其余自行完成）：谁是主角、文戏对白用什么语言、是否要求特定结局。

### 1.3 图片引用前缀（图生视频 I2VA 关键）
- 先统计最终会与提示词一起提交 H3 的全部图片，按**实际提交顺序**编号为 图片1、图片2、图片3…
- 存在图片时，提示词**绝对开头**用一整行连续引用：
  ```
  @图片1作为主角身份与服装参考@图片2作为对手身份与装备参考@图片3作为场景与光影参考
  ```
- 图片数量＝引用数量，每张只编号一次；一图多用途合并为一句（`@图片1作为人物身份、服装与场景参考`）。
- 九宫格图写为 `@图片1作为九宫格时序、构图与画面状态参考`。
- 引用行结束换行，下一行直接进入「生成一段……」正文；无图时第一行直接「生成一段……」。

### 1.4 全局生成规格（默认）
- 用户未指定时默认：**{5|10|15} 秒、16:9、2K、MiniMax H3、开启原生立体声**。
- 成品统一用**自然语言制作简报**格式（7 段）：
  1. 影片目标与核心概念
  2. 固定人物、主体、场景、道具与一致性
  3. 按时间连续覆盖 0—N 秒的画面和动作
  4. 剪辑、摄影、表演或动作规则
  5. 视觉风格与材质
  6. 声音设计
  7. 收束人物、动作、镜头与结尾一致性的正向结果
- **全正向描述**：只陈述「要呈现什么」，把约束转成人物锁定/动作因果/镜头规则/时间预算/画面结果，就地表达。成品始终以「生成什么」为核心。
- **时间段必须无缺口地覆盖完整时长**。连续 ≠ 一镜到底；默认允许硬切、匹配剪辑、遮挡切换、尺度跳切。连续感来自人物身份、空间轴线、动作余势、核心道具、运动方向、色彩母题或声音桥。仅用户明确要求才做一镜到底。
- 参考某作品时只提炼节奏/尺度/摄影/材质/情绪机制，不复制其角色、服装、怪物、标志性场景和专有名称。

### 1.5 流程 1：文戏模式
- **核心**：让 N 秒承载一次**清楚的关系变化**（识破/拒绝/背叛暴露/决定/失去…）。先定「触发前」与「结果出现后」的关系状态，再围绕一个证据/一个反应/一个行为结果组织。
- **生成机制**：核心证据或道具触发变化 → 用若干时间段完成（建立→证据/台词触发→即时决定→行为结果）；每次切镜由新信息/说话权/视线/道具状态/距离/行动决定触发，不按每句台词机械正反打；中近景/特写只留 2—3 个协同表演信号（目光停顿、呼吸、未完成手部动作、重心变化），微表情前后连续，不写抽象「他很悲伤」；对白写清说话人/原文/语言/质感/口型同步；枪击/受伤/拥抱/离开等结果必须在画面内完整发生；结尾留余波呈现后果。
- **输出**：一份中文 H3 自然语言提示词，含人物锁定、对白语言、0—N 秒时间线、表演与剪辑、视觉风格、声音设计、明确结尾结果。

### 1.6 流程 2：武戏模式
- **核心**：高速但可读的连续攻防；动作数量不是堆叠，**每个有效动作必须改变距离/姿态/控制权/环境/下一招路径**。
- **节奏预算**（见 §2 表）：开场起势极短、有效动作按秒数分级、单动作段含多个衔接动作、慢动作只用于决定胜负的接触帧、结尾呈现受力与卸力。
- **生成机制**：先锁双方轮廓/尺寸/装备/能力边界/惯用手/空间位置 → 设计短促战斗因果链（先手→交换→控制权反转→终结→落地）；上一招未收回时下一招已启动，受力直接成为翻越/旋转/位移/反斩动力；每次攻击写清起手方向/目标/接触位置/受力反应/下一动作来源，用具体动作取代「双方高速激战」等概括句；环境互动必须改变战局而非背景特效；每组连招保持一个主要运动方向，仅真实换向/借墙/重力改变才变轴线；切点落在接触/遮挡/落地/破障/方向匹配处；终结含命中＋对手物理反应＋主角卸力＋明确结果。
- **输出**：含双方与场景锁定、0—N 秒高速动作时间线、动作密度与速度规则、摄影原则、视觉材质、声音节拍、结果落地。

### 1.7 流程 3：九宫格模式
- **核心**：九格不是九张无关海报，也不是默认一镜到底；是九个清晰可辨的关键画面（九个镜头 / 九个动作峰值 / 同一主体九个演化状态）。连续感来自稳定主体/核心物件/材质继承/运动方向/色彩发展/构图匹配；允许硬切。
- **阶段 A 生成九宫格**：从故事/图片提取稳定主体＋N 秒视觉发展线 → 拆成左→右、上→下 1—9 格，每格任务不同不重复构图 → 覆盖「建立/触发/升级/第一次变化/中段/第二次升级/高潮/接近完成/最终画面」（武戏改九个动作峰值、文戏改九个关系节点）→ 生成 16:9、2048×1152 的 3×3 故事板（细中性分隔线）→ 同一主体身份/服装/装备/材质/配色贯穿九格，镜位/景别/姿态/动作明显变化。
- **阶段 B 依据实际成图生成 H3 提示词**：先观察实际输出再写；逐格记录主体姿态/镜位/道具/空间/色彩/差异；以实际九宫格为准，不沿用偏差旧分镜。明确 1—9 对应顺序；为九格分配无缺口时段；每格写「进入方式/主动作/离开方式」，前后格用动作方向/形状/颜色/道具/遮挡/声音关联；可硬切，不误写强制一镜到底；精确文字易失真时先表达字体造型与空间关系，可读文案交后期并标记需替换文字层。
- **交付**：① 九宫格图片或出图提示词；② 对应 H3 自然语言视频提示词。用户说「只要九宫格/先看图」则阶段 A 后暂停等确认。

### 1.8 修改规则 & 交付前自检
- 修改时先判断属「人物锁定 / 时间预算 / 动作因果 / 关系变化 / 九格映射 / 输出格式」，修正上游结构后重出完整成品。
- 「连续」默认＝叙事/动作/视觉关联（允许有关联硬切）；仅「长镜头/无剪辑/一镜到底」才改连续长镜头。
- 自检清单（节选）：已定流程？复用已给素材不重复要？人物/服装/装备/场景一致？时间段无缺口覆盖？文戏仅一次清楚关系变化+对白口型明确？武戏达动作密度+每次接触有受力与结果？九宫格九格全不同且依实际成图？连续来自可见锚点非强制一镜到底？输出为 H3 可直接复制的自然语言非 JSON？有图时绝对开头完整 `@图片N作为XX` 再换行进正文？约束已转正向描述？

---

## 2. 时长预算差异表（5s / 10s / 15s）

> 工坊「单镜头时长」选项 5/10/15 秒应与下表预算对齐。来源 S1/S2/S3。

### 2.1 文戏模式
| 项 | 5 秒 | 10 秒 | 15 秒 |
|---|---|---|---|
| 关系变化目标 | 一次立即可见的微型关系变化 | 一次清楚的关系变化 | 一次清楚的关系变化（最关键转折集中在 15s） |
| 时间段数 | 2—4 | 4—6 | 5—8 |
| 对白量 | 一句核心台词或两句极短交锋 | 两至四句短句 | 简短自然、能说完 |
| 结尾余波 | 0.3—0.6 秒 | 0.4—0.8 秒 | 0.5—1.2 秒（最后一秒仍给新情绪信息） |

### 2.2 武戏模式（节奏预算）
| 项 | 5 秒 | 10 秒 | 15 秒 |
|---|---|---|---|
| 开场起势 | 0.15—0.25 秒 | 0.20—0.35 秒 | ≤ 0.5 秒 |
| 有效动作数 | 6—10 | 10—16 | 14—20 |
| 单动作段（含衔接动作） | 0.8—1.4 秒 / 2—4 动作 | 1.2—2.0 秒 / 3—5 动作 | 1.5—2.5 秒 / 3—6 动作 |
| 慢动作 | 0—1 次，总 0.12—0.20 秒 | 0—2 次，总 0.20—0.35 秒 | 0—2 次，总 ≤ 0.6 秒 |
| 结尾状态 | 0.15—0.25 秒 | 0.20—0.30 秒 | ≤ 0.3 秒（定格） |

### 2.3 九宫格模式（时段分配）
| 项 | 5 秒 | 10 秒 | 15 秒 |
|---|---|---|---|
| 九格映射 | 1—3 建立 / 4—6 变化 / 7—9 结果 | 1—3 进入 / 4—6 核心变化 / 7—9 高潮与结果 | 1—9 无缺口覆盖，所有九格必须出现，中间格不得被形变吞掉 |
| 每格可视状态 | 九格作九个连续关键状态 | 每格约 0.8—1.3 秒 | 0—15 秒分配，每格形成可辨认视觉状态 |

---

## 3. 英文输出规范（I2VA / T2VA 三字段）

来源：S4/S5。工坊**默认输出英文**（H3 主要识别英文），用户可切「中文」查看。

### 3.1 字段顺序（base-en / I2VA）
- **指令行（仅 I2VA）**：
  ```
  For the target video, at 0.00 seconds into the target video, <Picture 1> (from [Shot 1]) is fully referenced. <Picture 2> and <Picture 3> are partially referenced to establish the product and environment.
  ```
- **三字段**（顺序固定）：
  ```
  integrated_multimodal_description: ...
  overall_soundscape: ...
  non_diegetic_music: ...
  ```
- **时间线**：`[Shot 1]` 起，后续 `At MM:SS.mmm`（如 `At 00:03.000`）。
- **中文台词**：`<d>[Chinese] … </d>`；画外音用 `The off-screen narrator (S1) says in a calm, reassuring voice: <d>[Chinese] … </d> while the subject's lips remain closed.`
- 对话说话人用 `(S1)` 等 ID；保留原语言写在 `<d>` 标签内。

### 3.2 与三流程的关系
- 三流程（§1）决定**叙事内容与时间预算**；英文三字段（§3）决定**提交给 H3 的格式外壳**。二者正交：先用 §1 设计镜头/动作/关系，再套 §3 格式输出。
- 工坊 `buildShotBriefEn`（data.js）已落地：I2VA 指令行＋三字段＋`[Shot N]` 时间戳＋中文 `<d>[Chinese]` 台词＋画外音嘴唇闭合＋`lead seamlessly into next shot` 衔接（可见画面状态）＋素材锚定。

---

## 4. 与 script-writing-studio 的结合点

来源：S6。已并入工坊英文生成逻辑。

- **素材锚定（ST）**：参考图在工坊里按 `<图1>/<图2>…` 标注用途，I2VA 指令行对应 `<Picture 1/2/3>`；图1 严格作第 0 秒首帧，图2+ 仅建产品/环境。
- **台词时长闸门**：中文 3—4 字/秒（强情绪）、4—5 字/秒（正常）、5—6 字/秒（快）；英文按音节近似。保证对白能在 N 秒内自然说完（对应 §2.1 对白量）。
- **段末状态 / 衔接**：每镜结尾写「自然衔接下一镜开头」的可见画面状态，非管理语言；对应三流程「连续来自可见锚点」。
- **防跑偏（CL/NP）**：风格/行业叙事 DNA 全片一次性注入，不逐镜重复；对应三流程「保持人物/服装/装备/场景一致」。
- **C 模板纯净版**：逐镜卡片保留各自 flavor（独立生成需要），整片六段式用纯净版避免重复。

---

## 5. 巨晴水疗产品合规语境

来源：S7（data.js 内置 hydro 行业）。

- 真实产品＝**巨晴水疗设备**（hydro 行业），目标＝更好宣传设备好处、让更多人知道、越来越健康。**不是仙侠剧**。
- 合规话术「**调理不治疗**」：使用 `healthKnowledge` / `conditioningPoints` / `industryNarrative` 中的养生合规语境，不承诺医疗疗效。
- 英文输出时行业叙事一次注入（见 §4 防跑偏），中文镜头文案不混入英文产品描述（用中文品牌兜底）。

---

## 6. 设计溯源表（每条规则 → 来源）

| 工坊特性 / 规则 | 来源 |
|---|---|
| 默认英文输出（state.lang='en'） | S4/S5（H3 主要识别英文） |
| 🔄 重新生成（本地、0 千问 token） | 用户需求 + S8 本地 generateStoryboard |
| 中文/英文切换 | S4/S5 双语文案 |
| I2VA 指令行 `<Picture 1> is fully referenced` | S4 官方示例 |
| 三字段 integrated/overall/non_diegetic | S5 base-en |
| `[Shot N]` + `At MM:SS.mmm` 时间戳 | S4/S5 |
| 中文台词 `<d>[Chinese]` + 画外音 lips remain closed | S4/S5 |
| 素材锚定 / 图1 首帧规则 | S1/S2/S3 图片引用前缀 + S6 ST |
| 时间段无缺口覆盖 / 连续≠一镜到底 | S1/S2/S3 全局规格 |
| 文戏一次关系变化 / 武戏动作密度 / 九宫格九格 | S1/S2/S3 三流程 |
| 时长预算（5/10/15 秒段数、动作数、余波） | S1/S2/S3 §2 |
| 台词时长闸门 / 段末状态 / 防跑偏 | S6 |
| 巨晴水疗合规「调理不治疗」 | S7 |
| 全正向描述结构 | S1/S2/S3 全局规格 |

---

## 7. 工坊当前落地情况 & 后续建议

**已落地（data.js / app.js）**：默认英文、中文切换、🔄 本地重新生成、I2VA 指令行＋三字段、素材锚定、台词时长闸门、防跑偏、巨晴合规语境、GitHub Pages 部署。

**建议（未做，可下一步）**：
1. **引入「三流程」模式选择 UI**（文戏/武戏/九宫格）——当前工坊只有「视频类型/风格」，缺叙事模式轴；接入后可按 §2 预算自动套用时间段数/动作密度/九格分配。
2. **时长预算自动校验**：选 5/10/15 秒时，按 §2 表提示文戏时间段数、武戏动作数上限，避免空镜或超密度。
3. **九宫格流程**：阶段 A 出图提示词 + 阶段 B 依实际成图生成（当前工坊未做九宫格分支）。
4. 本背书文档可随站部署，作为「提示词生成参考」公开页。

---

*合订版结束。任何生成异常，先回查本表 §6 溯源，再定位 data.js 对应函数。*
